import ReactDOM from 'react-dom/client';
import './css/index.css'
import './css/main.css'

import App from './App.jsx'

ReactDOM.createRoot(
  document.getElementById('root')
).render(<App />); 

// Hey Ray, if your reading this then that meand your about to add the backend code. 
// A code doesn't do anything, but should be ready to be connected to an API system
// Most Test Data comes from an JS file in the "test_data" folder